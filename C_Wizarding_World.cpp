#include <bits/stdc++.h>
using namespace std;
int solve(int a, int b, int c)
{
    int ans = 0;
    

    return ans;
}
int main()
{
    long int t;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        long long int a, b, c;
        cin >> a >> b >> c;
        int ans = 0;
        ans = solve(a, b, c);
        cout << ans << endl;
    }

    return 0;
}